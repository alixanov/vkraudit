# ── Frontend environment (copy to .env, опционально) ────────────────────
#
# Оставьте ПУСТЫМ для:
#   • локальной разработки (используется proxy на backend), и
#   • монолит-деплоя на Railway (backend сам раздаёт build, один origin).
#
# Заполните ТОЛЬКО при раздельном деплое frontend и backend как двух сервисов:
# REACT_APP_API_URL=https://your-backend.up.railway.app
REACT_APP_API_URL=
