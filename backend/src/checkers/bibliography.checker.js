// Проверка библиографии убрана — баллы перераспределены по главам и приложению.
export function checkBibliography(_parsed) {
  return { scores: { bibliographyScore: 0 }, referencesTotal: 0, issues: [] };
}
